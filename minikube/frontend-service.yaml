apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: frontend-cve
  name: frontend-cve
spec:
  type: NodePort
  ports:
    - name: "3000"
      port: 3000
      targetPort: 3000
      nodePort: 30300
  selector:
    io.kompose.service: frontend-cve
